alert("Welcome to the first release of the calculator! This calculator only supports addition.");

var firstNumber = Number(prompt('Enter the first number:'));
var secondNumber = Number(prompt('Enter the second number:'));

var result = firstNumber + secondNumber;

alert(`the summation result of ${firstNumber} + ${secondNumber} is ${result}`);
document.write(`the summation result of ${firstNumber} + ${secondNumber} is ${result}`)


